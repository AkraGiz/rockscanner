# Wet samples

Imported from `F:\WET`.

These samples are stored in `wet/` because wet surface condition takes precedence over the underlying tray or box type for this catalog. File names follow:

`crm_<hole>_<box>_<interval>_wet.jpg`

When the source file marks end of hole, the suffix `_eoh` is preserved.
