# Geological annotations and reference images

This folder includes samples where annotations, screenshots, generated/reference views, or cropped core strips are the dominant reason to keep the image separate from the canonical tray/box classes.

Imported from `F:\Mining`:

- `Z-821` interval samples with red markings.
- Screenshot/reference images.
- Depth-map/cropped-core reference images.
